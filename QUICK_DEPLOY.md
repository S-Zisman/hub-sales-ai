# ⚡ Быстрый деплой - краткая инструкция

## 🎯 Что нужно сделать

### 1. Откройте проект в Cursor

**File → Open Folder...** → выберите папку `hub-sales-ai`

Или через терминал:
```bash
cd "/Users/mymac/TRAINING/ВАЙБКОДИНГ (ДАМИР ХАЛИЛОВ)/MY PROJECTS/hub-sales-ai"
cursor .
```

### 2. Запустите автоматический деплой

```bash
./deploy.sh
```

Скрипт автоматически:
- ✅ Установит Docker на сервер
- ✅ Скопирует все файлы
- ✅ Установит зависимости

### 3. Завершите настройку на сервере

Подключитесь к серверу:
```bash
ssh root@164.92.248.246
# Пароль: CanadaChili2025$end
```

Выполните на сервере:

```bash
cd /opt/hub-sales-ai

# 1. Отредактируйте .env (добавьте Stripe ключи)
nano .env

# 2. Примените миграции БД
npm run db:migrate

# 3. Получите SSL сертификат (DNS должен быть настроен!)
certbot certonly --standalone -d api.sorokafes.com

# 4. Запустите проект
docker compose up -d

# 5. Проверьте логи
docker compose logs -f app
```

### 4. Настройте Stripe Webhook

1. Stripe Dashboard → Webhooks → Add endpoint
2. URL: `https://api.sorokafes.com/webhooks/stripe`
3. События: все subscription и invoice события
4. Скопируйте Secret → в `.env` как `STRIPE_WEBHOOK_SECRET`

### 5. Настройте DNS

В панели домена:
- `api.sorokafes.com` → A-запись на `164.92.248.246`
- `sorokafes.com` → A-запись на `164.92.248.246`

## ✅ Готово!

Бот должен работать на `@HUBSalesAI_bot`

---

**Подробная инструкция:** см. `DEPLOY_INSTRUCTIONS.md`

