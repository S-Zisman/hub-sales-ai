# 📚 Документация системы AI Business HUB

## А) Как работает функционал

### 1. Архитектура системы

Система состоит из следующих компонентов:

- **Telegram Bot** (`@HUBSalesAI_bot`) - ИИ-продавец на базе OpenAI GPT-4o-mini (fallback) или Claude 3.5 Sonnet
- **Stripe Integration** - Обработка платежей и подписок (Premium £17/мес, Test-Drive £9/мес)
- **PostgreSQL Database** - Хранение данных пользователей, подписок, истории диалогов
- **Redis** - Сессии бота (FSM) и очереди задач (BullMQ)
- **Nginx** - Reverse proxy для webhooks Stripe
- **Docker Compose** - Оркестрация всех сервисов

### 2. Воронка продаж (Finite State Machine)

Бот использует 4-этапную воронку продаж:

#### Этап 1: QUALIFICATION (Квалификация)
- Бот задает вопросы о бизнесе:
  - В какой нише работает бизнес?
  - Какой месячный оборот?
  - Сколько человек в команде?
- На этом этапе **продажа запрещена** - цель понять "Точку А" клиента
- На основе ответов рассчитывается `leadScore` (0-100):
  - Оборот > £50,000 → +30 баллов
  - Оборот > £10,000 → +20 баллов
  - Оборот > £5,000 → +10 баллов
  - Команда > 10 человек → +20 баллов
  - Команда > 3 человек → +10 баллов
- Статус в CRM обновляется: `NEW` → `WARM` или `QUALIFIED` (если score > 30)

#### Этап 2: PROBLEM_AMPLIFICATION (Усиление боли)
- Бот анализирует ответы пользователя на предмет упоминания проблем ("хаос", "проблем", "трудн")
- Использует технику **Challenger**: показывает последствия бездействия
- Экологично фрустрирует клиента, усиливая боль
- **Не предлагает решение** до тех пор, пока боль не усилена

#### Этап 3: SOLUTION_PRESENTATION (Презентация решения)
- На основе квалификации определяется продукт:
  - **Premium Club** (£17/мес) - если оборот > £10,000 ИЛИ команда > 3 человек
  - **Test-Drive** (£9/мес) - для остальных случаев (даунсейл)
- Бот генерирует персонализированное предложение через AI
- Создается динамическая ссылка на Stripe Checkout с промокодом:
  - Premium: `PREMIUM17`
  - Test-Drive: `SOROKA`

#### Этап 4: CLOSING (Закрытие)
- Пользователь переходит по ссылке и оплачивает в Stripe
- После успешной оплаты (`checkout.session.completed`):
  1. Создается запись в таблице `subscriptions` со статусом `ACTIVE`
  2. Генерируется **одноразовая ссылка** для входа в канал (через `createChatInviteLink` с `member_limit: 1`)
  3. Пользователь получает ссылку в личные сообщения
  4. Статус в CRM обновляется на `CUSTOMER`

### 3. Управление доступом к закрытому каналу

#### Kick Protocol (Протокол исключения)
При истечении подписки (`customer.subscription.deleted`):
1. **Ban**: Пользователь банится из канала (`banChatMember`)
2. **Unban**: Сразу же разбанивается (`unbanChatMember`) - это удаляет из черного списка, но не возвращает в канал
3. **Уведомление**: Пользователь получает сообщение о необходимости продлить подписку

#### Grace Period (Льготный период)
- При неудачной оплате (`invoice.payment_failed`):
  - Статус подписки → `PAST_DUE`
  - Пользователь получает уведомление с предупреждением
  - По умолчанию 3 дня на обновление платежной информации (настраивается в Stripe Smart Retries)

#### Одноразовые ссылки
- Каждая ссылка имеет `member_limit: 1` - работает только для одного пользователя
- Если ссылка передана другому, и тот вступит первым, плательщик не сможет войти
- Ссылка действительна 24 часа

### 4. Работа в закрытом канале

Бот отвечает в канале когда:
- Пользователь упоминает бота (`@HUBSalesAI_bot`)
- Пользователь отвечает на сообщение бота
- В канале публикуется пост с упоминанием бота

**Проверка доступа:**
- Бот проверяет наличие активной подписки у пользователя
- Если подписки нет → сообщение об отсутствии доступа
- Если подписка активна → бот отвечает на вопрос через AI

**Контекст диалога:**
- Бот использует последние 10 сообщений из истории диалога для контекста
- Все сообщения сохраняются в `conversation_logs`

### 5. Админ-панель CRM

Доступна через команды в боте (только для админов):

- `/admin` - Главное меню админ-панели
- `/stats` - Статистика: новые лиды, продажи, активные подписки, MRR, конверсия
- `/leads [статус]` - Список лидов (NEW, QUALIFIED, WARM, CUSTOMER, CHURNED, VIP)
- `/lead [telegram_id]` - Детальная карточка лида с подписками и историей
- `/hot` - Топ-10 лидов с высоким lead score
- `/conversation [telegram_id]` - Последние 10 сообщений диалога с лидом
- `/broadcast` - Рассылка сообщений по сегментам (все, не купили, члены клуба)
- `/add_access [telegram_id]` - Ручное добавление доступа к каналу (для VIP)
- `/remove_access [telegram_id]` - Ручное удаление доступа
- `/get_channel_id` - Инструкция по получению ID канала

**Супер-админы:**
- Настраиваются через `SUPER_ADMIN_ID` в `.env` (через запятую)
- Имеют доступ ко всем админ-функциям без записи в БД

### 6. Интеграция с Stripe

#### Webhook события:
- `checkout.session.completed` - Успешная оплата → активация подписки + отправка ссылки
- `customer.subscription.updated` - Обновление подписки (продление, изменение статуса)
- `customer.subscription.deleted` - Отмена подписки → Kick Protocol
- `invoice.payment_failed` - Неудачная оплата → уведомление + статус PAST_DUE
- `invoice.payment_succeeded` - Успешное продление → обновление периода

#### Динамические ссылки оплаты:
- Создаются через `stripe.checkout.sessions.create`
- В `client_reference_id` передается `telegram_id` для связи
- В `metadata` сохраняется тип продукта и промокод

#### Статические ссылки (опционально):
- Можно использовать статические ссылки Stripe через `USE_STATIC_STRIPE_LINKS=true`
- Настраиваются через `STRIPE_STATIC_PREMIUM_LINK` и `STRIPE_STATIC_TEST_DRIVE_LINK`

### 7. AI-модель

**Текущая конфигурация:**
- **Основная модель**: Claude 3.5 Sonnet (при проблемах с API)
- **Fallback**: OpenAI GPT-4o-mini (активен при `USE_OPENAI=true`)
- **Системный промпт**: Включает методологию SPIN, технику Challenger, правила ценообразования

**Промпт-инжиниринг:**
- Структурированный промпт в XML-формате
- Динамические инструкции в зависимости от этапа воронки
- Контекст пользователя (ниша, оборот, команда, боли) передается в промпт

---

## Б) Что делает бот из своей ДИ (Должностной инструкции)

### Основная роль: AI HUB Sales - Бизнес-консультант и продавец

#### 1. Квалификация лидов
- Задает ситуационные вопросы (ниша, оборот, команда)
- Выявляет проблемы и боли клиента
- Рассчитывает lead score для приоритизации
- Обновляет статус в CRM (NEW → WARM → QUALIFIED)

#### 2. Продажа по методологии SPIN
- **S**ituation (Ситуация) - понимает текущее состояние бизнеса
- **P**roblem (Проблема) - выявляет боли и проблемы
- **I**mplication (Последствия) - усиливает боль через технику Challenger
- **N**eed-payoff (Выгода) - показывает ценность решения

#### 3. Техника Challenger
- При упоминании "хаос" или проблем:
  - Показывает финансовые потери от бездействия
  - Экологично фрустрирует клиента
  - Создает срочность решения

#### 4. Персонализация предложения
- На основе квалификации определяет продукт:
  - Premium Club - для готовых к внедрению (оборот > £10k или команда > 3)
  - Test-Drive - для сомневающихся (даунсейл)
- Генерирует персонализированное предложение через AI

#### 5. Работа с возражениями
- Анализирует скрытые возражения в ответах клиента
- Формулирует ответы, продвигающие к следующему этапу воронки
- Не использует клише ("Рад помочь", "Отличный выбор")

#### 6. Консультирование в канале
- Отвечает на вопросы членов клуба
- Использует контекст предыдущих диалогов
- Предоставляет экспертные советы по бизнесу

#### 7. Соблюдение правил ценообразования
- Premium: £17/мес (вместо £57) только с промокодом PREMIUM17
- Test-Drive: £9/мес (вместо £13) только с промокодом SOROKA
- Никогда не предлагает цену до выявления боли

---

## В) Предварительная воронка пользователя в канал закрытого клуба

### Полный путь пользователя:

```
1. РЕКЛАМА / ЛЕНДИНГ
   ↓
   Пользователь видит рекламу или заходит на лендинг (Gamma)
   
2. ПЕРЕХОД В БОТА
   ↓
   Кнопка "Начать" → t.me/HUBSalesAI_bot?start=gamma_landing
   (startParam сохраняется для трекинга источника)
   
3. КВАЛИФИКАЦИЯ (/start)
   ↓
   Бот задает вопросы:
   - Ниша бизнеса
   - Месячный оборот
   - Размер команды
   → Рассчитывается leadScore
   → Статус: NEW → WARM/QUALIFIED
   
4. УСИЛЕНИЕ БОЛИ
   ↓
   Пользователь упоминает проблемы
   → Бот усиливает боль через Challenger
   → Статус: WARM/QUALIFIED
   
5. ПРЕЗЕНТАЦИЯ РЕШЕНИЯ
   ↓
   Бот предлагает продукт:
   - Premium (£17/мес) - если квалифицирован
   - Test-Drive (£9/мес) - если сомневается
   → Создается ссылка на Stripe Checkout
   
6. ОПЛАТА
   ↓
   Пользователь оплачивает в Stripe
   → Webhook: checkout.session.completed
   → Статус: CUSTOMER
   
7. АКТИВАЦИЯ ПОДПИСКИ
   ↓
   - Создается запись в subscriptions (ACTIVE)
   - Генерируется одноразовая ссылка (member_limit: 1)
   - Пользователь получает ссылку в личные сообщения
   
8. ВХОД В КАНАЛ
   ↓
   Пользователь переходит по ссылке
   → Вступает в канал "AI Business HUB - Клуб AI-предпринимателей"
   → Получает доступ к контенту и AI-менторам
   
9. РАБОТА В КАНАЛЕ
   ↓
   - Задает вопросы боту (@HUBSalesAI_bot)
   - Получает консультации
   - Участвует в мастер-классах
   - Использует материалы
   
10. ПРОДЛЕНИЕ / ОТМЕНА
    ↓
    - Автопродление через Stripe
    - При отмене → Kick Protocol (ban + unban)
    - Уведомление о необходимости продлить
```

### Критические точки воронки:

1. **Трекинг источника** - `startParam` сохраняется при `/start` (частично реализовано)
2. **Lead Score** - автоматический расчет на основе ответов
3. **Динамическое предложение** - выбор продукта на основе квалификации
4. **Одноразовые ссылки** - защита от "слива" контента
5. **Kick Protocol** - автоматическое исключение при истечении подписки

---

## Г) Что еще не реализовано из ТЗ

### 1. Защита платного контента на Gamma

**Требование ТЗ:**
> "Все ссылки, которые ведут на платные ресурсы - должны быть одноразовыми"

**Текущий статус:**
- ✅ Одноразовые ссылки для входа в канал реализованы
- ❌ Защита контента на Gamma не реализована

**Варианты реализации (из ТЗ):**
- **Вариант А (Простой)**: Платный контент доставляется в Telegram (PDF, тексты, видео)
- **Вариант Б (Продвинутый - Proxy Page)**: 
  - Создается защищенная страница на Gamma (с паролем)
  - Бот генерирует ссылку: `https://api.sorokafes.com/content/lesson1?token=XYZ`
  - Сервер проверяет токен и делает редирект на секретную страницу Gamma
  - Токен помечается как использованный или имеет короткий TTL

**Рекомендация ТЗ:** Начать с Варианта А (доставка в боте) или доставки в Закрытый Канал

### 2. Трекинг трафика с Gamma

**Требование ТЗ:**
> Кнопки на сайте Gamma должны вести на бота с параметром: `?start=gamma_landing`. Бот при старте считывает этот параметр и записывает источник трафика в CRM.

**Текущий статус:**
- ✅ Параметр `startParam` считывается при `/start`
- ✅ Проверка на `gamma_` префикс реализована
- ❌ Сохранение источника трафика в БД не реализовано

**Что нужно:**
- Добавить поле `source` или `utm_source` в таблицу `users`
- Сохранять источник при создании/обновлении пользователя
- Добавить фильтр по источнику в админ-панели (`/leads source:gamma`)

### 3. Автоматическая проверка истекших подписок (Cron Job)

**Требование ТЗ:**
> Реализовать автоматическую проверку подписок с истекшим `current_period_end` и запуск Kick Protocol.

**Текущий статус:**
- ✅ Kick Protocol реализован (вызывается через webhook)
- ❌ Автоматическая проверка (cron job) не реализована

**Что нужно:**
- Добавить периодическую задачу (через BullMQ или node-cron)
- Проверять подписки со статусом `ACTIVE` и `current_period_end < NOW()`
- Запускать `kickUserFromClub` для истекших подписок
- Обновлять статус подписки на `CANCELED`

### 4. Context Summary для экономии токенов

**Требование ТЗ:**
> В таблице `conversation_logs` должно быть поле `context_summary` - сжатое саммари контекста, генерируемое ИИ для экономии токенов.

**Текущий статус:**
- ✅ Таблица `conversation_logs` существует
- ✅ Поле `context_summary` есть в схеме Prisma
- ❌ Генерация саммари не реализована

**Что нужно:**
- При достижении определенного количества сообщений (например, 20)
- Генерировать саммари через AI (сжать предыдущий контекст)
- Сохранять в `context_summary`
- Использовать саммари вместо полной истории при превышении лимита токенов

### 5. Grace Period уведомления

**Текущий статус:**
- ✅ Уведомление при `invoice.payment_failed` реализовано
- ❌ Напоминания в течение grace period не реализованы

**Что нужно:**
- Отслеживать подписки со статусом `PAST_DUE`
- Отправлять напоминания:
  - Через 1 день после неудачной оплаты
  - Через 2 дня (последнее предупреждение)
  - Через 3 дня (финальное уведомление перед отменой)

### 6. Интеграция с Gamma через Proxy Page (опционально)

**Требование ТЗ:**
> Создать защищенную страницу на Gamma и проксировать доступ через сервер с проверкой токена.

**Текущий статус:**
- ❌ Не реализовано

**Что нужно:**
- Создать endpoint: `GET /content/:resourceId?token=XYZ`
- Проверять токен через `validateAccessLink`
- Делать редирект на защищенную страницу Gamma
- Помечать токен как использованный

### 7. Улучшения админ-панели

**Что можно добавить:**
- Экспорт данных в Excel/CSV
- Графики конверсии (визуализация воронки)
- Сегментация лидов по источникам трафика
- Автоматические теги на основе поведения
- A/B тестирование промптов

### 8. Мониторинг и аналитика

**Что можно добавить:**
- Дашборд с метриками в реальном времени
- Трекинг конверсии по этапам воронки
- Анализ эффективности промптов
- Мониторинг использования AI (токены, стоимость)
- Алерты при критических ошибках

---

## Итоговая таблица реализации

| Функционал | Статус | Приоритет |
|-----------|--------|-----------|
| FSM воронка продаж | ✅ Реализовано | Высокий |
| Stripe интеграция | ✅ Реализовано | Высокий |
| Kick Protocol | ✅ Реализовано | Высокий |
| Одноразовые ссылки (канал) | ✅ Реализовано | Высокий |
| Админ-панель CRM | ✅ Реализовано | Высокий |
| Работа в канале | ✅ Реализовано | Высокий |
| OpenAI fallback | ✅ Реализовано | Средний |
| Защита контента Gamma | ❌ Не реализовано | Средний |
| Трекинг трафика | ⚠️ Частично | Средний |
| Cron job для подписок | ❌ Не реализовано | Высокий |
| Context summary | ❌ Не реализовано | Низкий |
| Grace period напоминания | ❌ Не реализовано | Средний |
| Proxy Page для Gamma | ❌ Не реализовано | Низкий |

---

## Рекомендации по приоритетам

1. **Высокий приоритет:**
   - Cron job для автоматической проверки истекших подписок
   - Завершение трекинга трафика (сохранение источника в БД)

2. **Средний приоритет:**
   - Grace period напоминания
   - Защита контента Gamma (Вариант А - доставка в боте)

3. **Низкий приоритет:**
   - Context summary для экономии токенов
   - Proxy Page для Gamma (если потребуется)

