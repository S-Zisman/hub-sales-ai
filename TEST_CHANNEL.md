# 🧪 Тестирование работы бота в канале

## Шаг 1: Проверка прав бота

Убедитесь, что бот `@HUBSalesAI_bot` имеет права:
- ✅ Может публиковать сообщения
- ✅ Может редактировать сообщения

## Шаг 2: Отправка тестового сообщения

Отправьте в канал "AI Business HUB - Клуб AI-предпринимателей" сообщение:

```
@HUBSalesAI_bot Как увеличить продажи?
```

## Шаг 3: Проверка логов

Подключитесь к серверу и проверьте логи:

```bash
ssh root@164.92.248.246
cd /opt/hub-sales-ai
docker compose logs app --tail 50 | grep -E "UPDATE|CHANNEL_POST"
```

### Что должно появиться в логах:

1. **Если бот получает обновления:**
   ```
   [UPDATE] Channel update received: { updateType: 'channel_post', chatId: -1003645640766, ... }
   [CHANNEL_POST] Received update: { hasChannelPost: true, chatId: -1003645640766, ... }
   ```

2. **Если бот НЕ получает обновления:**
   - Логов не будет вообще
   - Это означает, что бот не получает `channel_post` события

## Шаг 4: Альтернативный способ (если channel_post не работает)

Если `channel_post` не работает, можно использовать **комментарии к постам**:

1. Включите комментарии в канале (если еще не включены)
2. Отправьте пост в канал
3. В комментариях к посту напишите: `@HUBSalesAI_bot вопрос`
4. Комментарии приходят как обычные `message` события

## Возможные проблемы:

### Проблема 1: Бот не получает channel_post
**Решение:** 
- Убедитесь, что бот имеет право "Может публиковать сообщения"
- Проверьте, что `allowed_updates` включает `'channel_post'`

### Проблема 2: Бот получает, но не отвечает
**Решение:**
- Проверьте логи на наличие ошибок
- Убедитесь, что `CLUB_CHANNEL_ID` правильный в `.env`
- Проверьте, что бот может отправлять сообщения в канал

### Проблема 3: Бот отвечает, но не в канале
**Решение:**
- Убедитесь, что используется `ctx.api.sendMessage(chatId, ...)` с правильным `chatId`
- Проверьте права бота на отправку сообщений

## Быстрая проверка прав бота:

Отправьте тестовое сообщение боту в личку:
```
/test_channel
```

Бот должен ответить с информацией о канале (если добавить такую команду).

