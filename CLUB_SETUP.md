# 🔐 Настройка закрытого клуба AI Business HUB

## ✅ Что уже сделано:

1. **Бот добавлен в администраторы канала** - правильно! ✅
2. **Код реализован:**
   - `inviteUserToClub()` - создание одноразовой ссылки при оплате
   - `kickUserFromClub()` - удаление при истечении подписки (Kick Protocol)
   - Автоматическая проверка истекших подписок (каждый час)
   - Обработка webhook'ов Stripe

## 📋 Что нужно сделать:

### 1. Получить ID канала

ID канала нужен для работы функций приглашения и удаления.

**Способ 1 (рекомендуется):**
1. Добавьте бота @userinfobot в канал как администратора
2. Бот покажет ID канала

**Способ 2:**
1. Перешлите любое сообщение из канала боту @getidsbot
2. Бот покажет ID канала

**Способ 3:**
1. Используйте команду `/get_channel_id` в боте (если вы админ)
2. Следуйте инструкциям

**Формат ID:** ID канала начинается с `-100` (например: `-1001234567890`)

### 2. Настроить CLUB_CHANNEL_ID

После получения ID канала, добавьте в `.env` на сервере:

```bash
CLUB_CHANNEL_ID=-1001234567890
```

Замените `-1001234567890` на реальный ID вашего канала.

### 3. Перезапустить бота

```bash
cd /opt/hub-sales-ai
docker compose restart app
```

## 🎯 Как работает функционал:

### При успешной оплате (Stripe webhook):

1. Создается подписка в базе данных
2. Статус пользователя меняется на `CUSTOMER`
3. **Автоматически создается одноразовая ссылка** для входа в канал
4. Ссылка отправляется пользователю в личные сообщения
5. Ссылка работает только для одного пользователя (`member_limit: 1`)

### При истечении подписки:

1. **Автоматическая проверка каждый час** - ищет истекшие подписки
2. При обнаружении истекшей подписки:
   - Пользователь удаляется из канала (ban + unban)
   - Подписка помечается как `CANCELED`
   - Статус пользователя меняется на `CHURNED`
   - Отправляется уведомление пользователю

### При отмене подписки (Stripe webhook):

1. Получается событие `customer.subscription.deleted`
2. Пользователь сразу удаляется из канала
3. Отправляется уведомление

## 🔧 Админские команды для управления доступом:

### `/add_access [telegram_id]`
Ручное предоставление доступа пользователю:
- Создает подписку на 30 дней
- Отправляет пригласительную ссылку
- Обновляет статус на `CUSTOMER`

**Пример:** `/add_access 199140013`

### `/remove_access [telegram_id]`
Ручное удаление доступа:
- Отменяет все подписки
- Удаляет пользователя из канала
- Обновляет статус на `CHURNED`
- Отправляет уведомление

**Пример:** `/remove_access 199140013`

### `/get_channel_id`
Показывает инструкции по получению ID канала или текущий ID, если настроен.

## ⚠️ Важные моменты:

1. **Бот должен быть администратором канала** - иначе не сможет создавать ссылки и удалять пользователей
2. **Одноразовые ссылки** - каждая ссылка работает только для одного пользователя
3. **Kick Protocol** - использует `banChatMember` + `unbanChatMember` для возможности повторного входа после оплаты
4. **Grace Period** - по умолчанию 3 дня после истечения подписки (настраивается в `config.business.gracePeriodDays`)

## 🧪 Тестирование:

1. **Тест приглашения:**
   - Используйте `/add_access [ваш_telegram_id]`
   - Проверьте, что ссылка пришла
   - Проверьте, что ссылка работает только один раз

2. **Тест удаления:**
   - Используйте `/remove_access [ваш_telegram_id]`
   - Проверьте, что вы удалены из канала
   - Проверьте, что можете вернуться по новой ссылке

3. **Тест автоматической проверки:**
   - Создайте тестовую подписку с истекшей датой
   - Подождите до следующего часа (или измените интервал в коде)
   - Проверьте, что пользователь удален

## 📝 Чеклист настройки:

- [ ] Бот добавлен в администраторы канала
- [ ] Получен ID канала
- [ ] `CLUB_CHANNEL_ID` добавлен в `.env`
- [ ] Бот перезапущен
- [ ] Протестирована команда `/get_channel_id`
- [ ] Протестирована команда `/add_access`
- [ ] Протестирована команда `/remove_access`
- [ ] Протестирована оплата через Stripe (webhook)
- [ ] Протестировано автоматическое удаление при истечении подписки

